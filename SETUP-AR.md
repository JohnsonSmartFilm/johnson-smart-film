# دليل التفعيل — Johnson Smart Film (خطوة بخطوة)

اتبع الخطوات دي بالترتيب، هتاخد منك حوالي 15-20 دقيقة، ومرة واحدة بس.

---

## 1) اعمل حساب Supabase (مجاني)

1. روح على https://supabase.com واعمل حساب (تقدر تدخل بجيميل).
2. اضغط **New Project**.
3. اختار اسم للمشروع (مثلاً `johnson-smart-film`) وحط باسورد قوي لقاعدة البيانات (احفظه في مكان آمن).
4. اختار المنطقة الأقرب لمصر (مثلاً Frankfurt/Europe) واضغط **Create**.
5. استنى دقيقة لحد ما المشروع يخلص إعداد.

---

## 2) شغّل ملف قاعدة البيانات

1. من القائمة الجانبية، افتح **SQL Editor**.
2. اضغط **New query**.
3. افتح الملف `supabase/schema.sql` الموجود في مجلد المشروع، وانسخ **كل محتواه**.
4. الصقه في محرر SQL واضغط **Run**.
5. هيظهرلك "Success" — يبقى كل الجداول والصلاحيات اتعملت تلقائي.

---

## 3) هات مفاتيح الاتصال (API Keys)

1. من القائمة الجانبية: **Project Settings → API**.
2. هتلاقي:
   - **Project URL**
   - **anon public key**
3. افتح ملف `js/supabase-client.js` في المشروع، وحط القيم دي مكان:
   ```js
   window.SUPABASE_URL = 'YOUR_SUPABASE_URL';
   window.SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
   ```

---

## 4) امنع طلب تأكيد الإيميل (مهم جداً)

عشان لما تعمل حساب لعميل يقدر يدخل فوراً بالإيميل والباسورد اللي انت حددتهملهم من غير ما ينتظر إيميل تأكيد:

1. **Authentication → Providers → Email**.
2. دوّر على خيار **"Confirm email"** وقفّله (Disable).
3. احفظ.

---

## 5) اعمل أول حساب أدمن (حسابك انت)

1. **Authentication → Users → Add user**.
2. حط الإيميل والباسورد بتاعينك، وفعّل **Auto Confirm User**.
3. اضغط Create، وانسخ الـ **UID** بتاع اليوزر اللي اتعمل.
4. روح تاني على **SQL Editor** ونفذ (بعد ما تغيّر القيم):
   ```sql
   insert into public.profiles (id, role, full_name, email)
   values ('الصق-الـ-UID-هنا', 'admin', 'اسمك', 'الإيميل بتاعك');
   ```
5. كده حساب الأدمن جاهز وتقدر تدخل بيه على `/login`.

---

## 6) رقم الواتساب بتاع الحجوزات

افتح `js/booking.js` وغيّر الرقم في أول سطر:
```js
const BUSINESS_WHATSAPP = '201114171416'; // غيّره لرقمك
```

---

## 7) ارفع الموقع

نفس طريقة الاستضافة اللي شغال بيها الموقع حالياً (GitHub Pages حسب ملف CNAME الموجود) — ارفع كل الملفات زي ما هي، مفيش حاجة إضافية مطلوبة، الموقع كله لسه Static وشغال على نفس الاستضافة.

---

## إزاي تستخدم النظام بعد كده

- **إنشاء عميل جديد**: من `/admin.html` → Customers → Add Customer → حط الاسم والإيميل والباسورد (أو "Generate" لباسورد عشوائي) → ابعتهملهم يدخلوا بيهم على `/login`.
- **إضافة سيارة/خدمة لعميل**: من صفحة تفاصيل العميل (اضغط على أيقونة العين 👁 جنب اسمه).
- **تغيير حالة خدمة** (Pending → In Progress → Completed): من تاب "All Services" — العميل هياخد إشعار تلقائي فوراً في الداشبورد بتاعته.
- **الحجوزات الجديدة** من فورم الموقع بتظهر في تاب "Booking Requests"، وتقدر تحولها لعميل كامل بضغطة زرار واحدة (👤+) وهيتعمل تلقائي أول سيارة وأول خدمة ليه من بيانات الحجز.

---

## ملاحظات مهمة

- **الواتساب**: حالياً بيفتح شات واتساب بكل تفاصيل الحجز جاهزة، وانت بس تضغط إرسال. لو حبيت لاحقاً إرسال تلقائي 100% من غير أي تدخل، ده محتاج WhatsApp Business API الرسمية من ميتا (بتاخد أيام للتوثيق) — قولّي وقتها وهظبطهالك.
- **الأمان**: إنشاء الحسابات بيتم من المتصفح مباشرة (Client-side) وده حل عملي وشائع للمشاريع الصغيرة/المتوسطة. لو حبيت مستوى أمان أعلى مستقبلاً (يمنع أي حد غير الأدمن من محاولة إنشاء حسابات حتى لو حاول يدخل من الـ Console)، ده بيتحول لـ Supabase Edge Function — تفصيلة تقدر تطلبها في أي وقت.
- **كل كود تلقائي**: كل عميل، كل عربية، وكل خدمة بياخدوا كود فريد تلقائي (مثال: `JSF-CUS-A1B2C3`, `JSF-VEH-...`, `JSF-SRV-...`) وده بيتعمل لوحده من قاعدة البيانات، مفيش حاجة تعملها يدوي.
